services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: dating_bot
      POSTGRES_USER: dating
      POSTGRES_PASSWORD: dating
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dating -d dating_bot"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  bot:
    build: .
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql+asyncpg://dating:dating@postgres:5432/dating_bot
      REDIS_URL: redis://redis:6379/0
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    command: sh -c "alembic upgrade head && python scripts/seed_cities.py && python -m app.main"
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
