[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c \"alembic upgrade head && python scripts/seed_cities.py && python -m app.main\""
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
